<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DairyManagement">

        <!-- Login (Launcher Activity) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".DashboardActivity" android:exported="false" />
        <activity android:name=".AddMemberActivity" android:exported="false" />
        <activity android:name=".MilkEntryActivity" android:exported="false" />
        <activity android:name=".ViewMilkActivity" android:exported="false" />
        <activity android:name=".DateReportActivity" android:exported="false" />
        <activity android:name=".MemberTotalActivity" android:exported="false" />
        <activity android:name=".SelectMemberActivity" android:exported="false" />

    </application>

</manifest>